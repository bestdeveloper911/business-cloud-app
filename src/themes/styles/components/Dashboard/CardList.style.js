import { StyleSheet } from 'react-native';

export default colors =>
  StyleSheet.create({
    card_title: {
      fontSize: '12px',
      textAlign: 'center'
    },
    card_content: {
      justifyContent: 'center',
    }
  });
